# Medcare ML Service — Python Requirements
# Install with: pip install -r requirements.txt --break-system-packages

# Core framework
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# ML stack
scikit-learn>=1.3.0
numpy>=1.24.0
joblib>=1.3.0

# OpenAI (optional — falls back to rule-based if not installed)
openai>=1.0.0

# Production hardening
flask-limiter>=3.5.0     # Rate limiting
gunicorn>=21.0.0         # Production WSGI server

# Dev/testing
pytest>=7.0.0
